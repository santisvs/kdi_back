packages:
  yum:
    # Nota: En Amazon Linux 2023, postgresql-devel puede no estar disponible
    # Como usamos psycopg2-binary (que incluye las librerías), no necesitamos postgresql-devel
    # Solo necesitamos gcc y python3-devel para compilar algunas extensiones Python
    gcc: []
    python3-devel: []

